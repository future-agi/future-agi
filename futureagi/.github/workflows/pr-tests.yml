# MOVED — this file is inert.
#
# GitHub Actions only reads workflows from the repository-root .github/workflows
# directory, so this nested copy never ran (and it was fully commented out).
#
# Backend CI now lives at: <repo-root>/.github/workflows/backend-ci.yml
#   - lint (ruff + black) runs on every backend PR
#   - the full docker-compose test suite runs via workflow_dispatch (manual)
