"""DRF bridge registrations for model_hub-side optimization (Phase 2A Packet E).

Pre-provisioned by Packet A so Packet E can extend DatasetOptimizationViewSet
@actions here (NOT in _misc_viewsets.py — additive registration per A10).
"""
