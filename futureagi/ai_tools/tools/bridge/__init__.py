# Bridge tools are now registered via @expose_to_mcp decorators on ViewSet classes.
# No manual imports needed — the decorator auto-registers tools when the ViewSet is imported.
#
# See: tracer/views/project.py for the first example.
