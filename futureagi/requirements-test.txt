# Test and development dependencies.
#
# Layered on requirements.txt rather than compiled as a second full lock, so the
# two cannot drift apart. They did: this file was previously generated with
#
#     uv pip compile pyproject.toml --extra dev -o requirements-test.txt
#
# which had two problems. It was 123 packages behind requirements.txt (missing
# structlog, redis, temporalio, webauthn, pyotp, qrcode, retell-sdk,
# clickhouse-connect, chevron, pdfplumber and others, several of which are in
# the auth/ROOT_URLCONF import chain, so any one of them failed every
# API-touching test at import). And `--extra dev` reads only
# [project.optional-dependencies].dev; the pytest stack lives in
# [dependency-groups].dev, which needs `--group dev`, so this file had never
# contained pytest at all.
#
# Runtime pins come from requirements.txt. Keep the toolchain below in sync with
# [project.optional-dependencies].dev and [dependency-groups].dev in
# pyproject.toml. Entries already pinned in requirements.txt (grpcio,
# grpcio-tools, django-stubs, djangorestframework-stubs, types-pyyaml) are not
# repeated here so the pinned version wins.

-r requirements.txt

# [project.optional-dependencies].dev
pre-commit>=4.2.0
mypy>=1.8.0
types-requests
types-redis

# [dependency-groups].dev
pytest>=8.4.1
pytest-asyncio>=0.21.0
pytest-cov>=6.2.1
pytest-django>=4.11.1
pytest-mock>=3.14.1
pytest-split>=0.11.0
pytest-xdist>=3.8.0
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
