// The "Scenarios to generate" field's default, seeded into each build panel's
// form. The backend owns the min/max (deployment-settable), so the UI keeps no
// range of its own.
export const DEFAULT_SCENARIOS = "10";
