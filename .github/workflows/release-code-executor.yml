# Manual escape hatch for ad-hoc and -dev builds. Stable releases go through
# release-images.yml (tag-triggered); this wrapper shares the same build via
# build-image-multiarch.yml (native linux/amd64 + linux/arm64, registry layer
# cache) so the two paths cannot drift. The size budget only reports here: an
# ad-hoc build is never blocked by it.
#
# The image builds FROM the base its Dockerfile pins (ARG CODE_EXECUTOR_BASE).
# If that tag is not published yet, publish it first with base-image-publish.yml
# (image: code-executor-base, the pinned version) from the same branch.
name: Release code-executor

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to publish (e.g. v2.1.0-dev)'
        required: true
        type: string
      branch:
        description: 'Branch to build from'
        required: true
        default: 'main'
        type: choice
        options: [main, dev]

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: read
      packages: write # ghcr build cache
    uses: ./.github/workflows/build-image-multiarch.yml
    with:
      image: futureagi/code-executor
      context: ./futureagi/code-executor
      dockerfile: ./futureagi/code-executor/Dockerfile
      version: ${{ inputs.version }}
      ref: ${{ inputs.branch }}
      push-latest: false
      size-budget: report
      free-disk: false
      # Own cache scope, reading the release's: an ad-hoc build of another
      # branch must not replace the cache the next release reuses.
      cache-scope: adhoc-code-executor
      cache-from-extra: |
        ghcr.io/future-agi/future-agi/build-cache:code-executor-amd64
        ghcr.io/future-agi/future-agi/build-cache:code-executor-arm64
    secrets:
      dockerhub-username: ${{ secrets.DOCKERHUB_USERNAME }}
      dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}
